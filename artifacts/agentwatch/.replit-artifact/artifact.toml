kind = "web"
previewPath = "/"
title = "AgentWatch Dashboard"
version = "1.0.0"
id = "artifacts/agentwatch"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24937

[services.development]
run = "pnpm --filter @workspace/agentwatch run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/agentwatch", "run", "build" ]
publicDir = "artifacts/agentwatch/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24937"
BASE_PATH = "/"
